在2.14修改ch5文档过程中出现了如下的情况：
**1.**

初步测试：电脑风扇狂转，初步判断是进程死循环

ai分析并且查看相关的代码后：

- 在 main.rs 的系统调用处理逻辑中，存在一个严重问题：当系统调用处理完成后，没有正确恢复进程执行状态。特别是exec系统调用后，应该直接开始执行新程序，而不是返回用户态。

- 进程管理器的调度逻辑可能导致死循环。当没有进程可执行时，代码会进入无限循环而不是正确关机。

- exec调用后没有正确设置返回值和上下文，导致进程无法正常执行新程序。

**2.**

在测试修改文档的过程中发现仅仅只是填补文档难以真正的学习内核，所以在接下来我会去主动手写而非ai代写，并且把相关的导入库下载并且修改重构，使其成为一个真正的符合自己学习轨迹和思路的内核

而且我又有了一个新的创想：
从ch1-...的版本中，每一次的迭代我都会写出到底在哪些文件中增添了哪些代码，通过具体的例子来绘制我的实验md而不是在一个md中陈列，总md介绍项目主题，副的小的md分布在各个文件夹中介绍增添的代码和感想。